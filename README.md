# 🧪 Test Data Generator (MVP)

A Flask-based tool that automatically generates **synthetic test data** from user-defined **metadata templates** — no manual scripting needed.  
It supports **primary/foreign key relationships**, **realistic fake values**, and **easy CSV exports** for testing data pipelines.

---

## 🚀 Key Features

- **Metadata-driven**: Upload CSVs defining columns, data types, formats, and constraints.
- **Rich data types**: Supports string, integer, decimal, date, and timestamp with validation.
- **Realistic values** via [Faker](https://faker.readthedocs.io/) — names, emails, addresses, currencies, etc.
- **Primary/Foreign key linking** between tables.
- **Web UI** for generation, preview, and downloads.
- **User manual** with guided screenshots.
- **Excel macro** for easy metadata creation.
- **Automatic logging** of generation steps.

---

## Installation needed
Flask
pandas
faker
python-dateutil

---

## Execution
Run the .bat file

